# Timezone (optional)
TZ=Europe/Rome

# ----- Audio (Pulse / PipeWire Pulse) -----
# For Linux user id 1000; change 1000 → your actual uid if different
XDG_RUNTIME_DIR=/run/user/1000
PULSE_DIR=${XDG_RUNTIME_DIR}/pulse
PULSE_SERVER=unix:${PULSE_DIR}/native

# ----- X11 (default) -----
DISPLAY=:0

# ----- Wayland (use with `--profile wayland`) -----
WAYLAND_DISPLAY=wayland-0
# QT_QPA_PLATFORM=wayland  # uncomment if needed by your Qt build
